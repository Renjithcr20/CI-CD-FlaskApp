apiVersion: v1
kind: Service
metadata:
    name: flask-app-service
    labels:
        app: flask-app
spec:
    type: LoadBalancer
    selector:
        app: flask-app
    ports:
    - port: 5000
      targetPort: 80
      protocol: TCP
      name: http